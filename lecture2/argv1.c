#include <cs50.h>
#include <stdio.h>


//
int main(int argc, string argv[])
{
    // iterate over the strings in argv
    for (int i = 0; i < argc; i++)
    {
        //iterate over the characters in i'th
        for (int j = 0; n = strlen(argv[i]); j < n; j++)
        {
            //print the j'th character in the i'th string
            printf("%c\n", argv[i][j]);
        }
    }
}