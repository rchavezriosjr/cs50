#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main (string k)
{
    for (int j = 0, n = strlen(k); j < n; j++)
    {
        // Check to see if the character is uppercase
        if (isupper(k[j]))
        {
            // Convert ascii value to alphabetical index value
            int upperAlphaIndex = p[i] - 65;
            printf("%c", c);
        }
        // Otherwise, character is lowercase
        else
        {
            // Convert ascii value to alphabetical index value
            int lowerAlphaIndex = k[j] - 97;
            printf("%c", c);
        }
    }
}